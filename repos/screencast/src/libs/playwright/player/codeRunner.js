const { NodeVM } = require('vm2')

class CodeRunner {

  /**
   * Player Class instance
   */
  player = undefined
  
  constructor(player) {
    this.player = player
  }
  
  /**
   * Runs the code passed to it via the player
   */
  run = async () => {
    
  }
  
}