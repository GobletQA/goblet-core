const { Logger } = require('@keg-hub/cli-utils')
const { get, noOpObj } = require('@keg-hub/jsutils')

const authToken = app => {
  return ({ data, socket }) => {}
}

module.exports = {
  authToken,
}
