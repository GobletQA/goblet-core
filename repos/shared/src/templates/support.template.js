/**
 * Support Template
 * Support files are loaded when feature files are executed
 * This allows them to be imported used just like any other script
 * @example
 * const doSomeThingHelper = () => { ...exec some code }
 * module.exports = { doSomeThingHelper }
 *
 */
